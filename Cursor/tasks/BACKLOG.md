# BACKLOG

Future work not yet scheduled. Do not implement without explicit approval.

1. Define first capability plugin specification (e.g. rss or static-cache)
2. Implement `tools/core/` validation scripts
3. Extract stable subset to `HADA_Website_Template` release repo
4. Create sample site in `HADA_Website_Template_Sample`
5. Register external references in project registry (local paths only)
