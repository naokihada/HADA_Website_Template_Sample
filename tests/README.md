# Tests

Automated and manual test definitions for framework tools and plugins.

Foundation phase: directory placeholder. Add tests when `tools/core/` scripts exist.

Record results in `Cursor/reports/TEST.md`.
